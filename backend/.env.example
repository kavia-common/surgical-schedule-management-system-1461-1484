# Database connection (for future persistence integration)
# For MongoDB (example):
MONGODB_URI=mongodb://localhost:27017/surgical_schedule

# Server
PORT=3000
HOST=0.0.0.0

# Node environment
NODE_ENV=development
